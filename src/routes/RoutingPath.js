const homeView = "/"
const showsView = "/shows"
const signInView = "/signin"
const infoView = "/info"

const RoutingPath = {
    homeView,
    showsView,
    signInView,
    infoView
}

export default RoutingPath