const homeView = "/"
const showsView = "/shows"
const signInView = "/signin"
const infoView = "/info"
const searchView = "/search"

const RoutingPath = {
    homeView,
    showsView,
    signInView,
    infoView,
    searchView
}

export default RoutingPath