const homeView = "/"
const favouriteView = "/favourites"
const signInView = "/signin"
const infoView = "/info"

const RoutingPath ={
    homeView,
    favouriteView,
    signInView,
    infoView
}

export default RoutingPath